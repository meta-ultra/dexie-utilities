import { type ApplicationDexie } from "../../db";
import { I{{~upperCamelCase entityName~}}TableName } from "./I{{~upperCamelCase entityName~}}";
import { mock, Random } from "mockjs";

export async function populateI{{~upperCamelCase entityName~}}(db: ApplicationDexie) {
  const $table = db[I{{~upperCamelCase entityName~}}TableName];
  await $table.clear();

  const data = [];

  await $table.bulkAdd(data);
}