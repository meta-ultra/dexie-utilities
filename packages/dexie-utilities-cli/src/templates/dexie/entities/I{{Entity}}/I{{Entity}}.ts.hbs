export const I{{~upperCamelCase tableName~}}TableName = "{{tableName}}";
export const I{{~upperCamelCase tableName~}}Schema = "{{getDexieSchema $dexie}}";
export type I{{~upperCamelCase tableName~}}KeyType = {{{getDexiePrimaryKeyType $dexie}}};

export interface I{{~upperCamelCase tableName}} {
  {{#each $dexie}}
    {{@key}}{{#unless this.required}}?{{/unless}}: {{this.type}};
  {{/each}}
}